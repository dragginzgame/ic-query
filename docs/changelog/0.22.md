# 0.22 Changelog

Detailed release notes for the `0.22.x` release line.

Root summary: [../../CHANGELOG.md](../../CHANGELOG.md)

When a patch introduces any new CLI surface, include a fenced `bash` command
example here and in the root changelog. New CLI surface includes commands,
subcommands, options, option values, and new supported option combinations.
Cleanup-only patches without CLI behavior changes should not include command
examples.

## 0.22.6 - 2026-08-01

### Changed

- Replaces the 865-line mixed Dashboard source module with an explicit facade
  over canister, metric, and network-resource source owners.
- Keeps source provenance matching, fixed Dashboard authority projection, and
  typed invalid-source and invalid-request construction in one shared facade.
- Moves each public capability trait beside its request normalization, source
  validation, canonical ordering, and report projection while preserving the
  existing family-root re-exports.
- Keeps principal and module-hash validation canister-specific, metric series
  and observation bounds metric-specific, and daily-statistics, coordinate,
  and node-count validation network-specific.

### Compatibility

- This is an internal ownership change. Existing public source-trait paths,
  report builders, custom-source contracts, typed errors, validation order and
  messages, canonical ordering, report JSON and text, and network behavior are
  unchanged.
- No CLI surface, compatibility shim, alias, cache behavior, hidden follow-up
  request, or mutation behavior is added.

## 0.22.5 - 2026-08-01

### Changed

- Replaces the 836-line mixed Dashboard live adapter with an explicit facade
  over canister, metric, and network-resource capability modules.
- Keeps the Reqwest client, response handling, base-endpoint validation, and
  path-segment mechanics in one shared transport owner.
- Moves canister lookup and bounded collection URLs and wire DTOs, metric URL
  and raw-series decoding, and daily-statistics and boundary-node resource
  handling beside their respective `LiveIcSource` trait implementations.
- Moves the existing twelve URL and additive-wire-field tests beside the
  capability implementation they exercise.

### Compatibility

- This is an internal ownership change. Existing `LiveIcSource`, public source
  traits and report builders, endpoint defaults, validation-before-HTTP order,
  request bounds, report JSON and text, and network-call counts are unchanged.
- No CLI surface, compatibility shim, alias, cache behavior, hidden follow-up
  request, or mutation behavior is added.

## 0.22.4 - 2026-08-01

### Changed

- Replaces the 894-line mixed ICRC text renderer with an explicit facade over
  account, ledger-history, and ledger metadata/evidence renderers.
- Keeps account identity and amount presentation with account and cached
  account-history reports; ledger block, block-type, and archive tables with
  transaction history; and token metadata, index discovery, capabilities, and
  tip-certificate evidence with ledger metadata.
- Retains only the shared optional table-section wrapper and common two-column
  alignment in the facade instead of introducing a broader renderer
  abstraction.

### Compatibility

- This is an internal ownership change. Existing `ic_query::icrc::*` renderer
  paths, feature availability, text bytes, report JSON, cache paths and
  schemas, CLI behavior, and network calls are unchanged.
- No CLI surface, compatibility shim, alias, cache migration, hidden live call,
  or mutation behavior is added.

## 0.22.3 - 2026-08-01

### Changed

- Replaces the 1,117-line mixed ICRC contract module with a small explicit
  facade over separate request and report/row modules.
- Gives request constructors, cache identities, refresh controls, and list-view
  options one owner independent of serialized report evidence.
- Keeps report envelopes, cache summaries, refresh-attempt status, and
  transaction/archive/capability rows together as the serialized response
  contract owner.

### Compatibility

- This is an internal ownership change. Existing `ic_query::icrc::*` public
  paths, feature availability, fields, derives, constructors, report JSON,
  text rendering, cache paths and schemas, and network behavior are unchanged.
- No CLI surface, compatibility shim, alias, cache migration, hidden live call,
  or mutation behavior is added.

## 0.22.2 - 2026-08-01

### Changed

- Replaces the 1,495-line mixed ICRC account-transaction live module with
  cohesive owners for live query orchestration, arbitrary-size cursor
  validation, complete paged collection state, and index wire handling.
- Keeps the generic ICRC index Candid response and the deployed ICP index
  Candid response in separate wire modules. Their same-method fallback decoder
  shares only the request envelope, codec selection, normalized transaction
  summary, and small JSON projection helpers.
- Moves collection progress and partial-failure evidence beside the state that
  owns page counts, retained rows, and cursors. Wire conversion remains unaware
  of refresh limits, cache publication, and progress reporting.
- Keeps the existing focused fixtures with their new owners, covering
  arbitrary-Nat cursors, canonical/exhausting collection, duplicate and
  changing-history rejection, resolved-index failure evidence, generic approve
  projection, and ICP account-identifier projection.

### Compatibility

- This is an internal ownership change. Public Rust paths and types, report
  JSON, raw transaction fields, cache paths and schemas, pagination and cursor
  semantics, index discovery and ledger-identity validation, progress events,
  and network calls are unchanged.
- Generic ICRC index and deployed ICP index decoding remain separate protocol
  contracts; neither is treated as an alias or compatibility reader for the
  other.
- No CLI surface, compatibility shim, cache migration, hidden live call, or
  mutation behavior is added.

## 0.22.1 - 2026-08-01

### Changed

- Replaces the top-level, family, group, and leaf staged parsers with one
  composed Clap command tree whose typed matches feed report dispatch directly.
- Removes the reduced passthrough command definitions, raw help/version and
  global-option scans, duplicated staged parse helpers, and hidden
  `--__icq-network` forwarding option. The selected network is instead passed
  explicitly from the root match tree to NNS, SNS, and system commands.
- Collapses the identical ICRC token, index, block-types, tip-certificate, and
  capabilities dispatch paths behind one typed ledger-report runner.
- Adds structural coverage proving every declared command path supports native
  help without entering report dispatch and every report leaf retains the
  shared `--json` flag, alongside the existing option, error, no-live-call, and
  binary smoke coverage.

### CLI

- Clap now owns `-h`/`--help`, nested `help <path>`, propagated
  `-V`/`--version`, required-subcommand diagnostics, and invalid-option output.
  Required command groups are consequently shown as `<COMMAND>`, and native
  help options are present in generated leaf help.

  ```bash
  icq help nns topology
  ```

### Compatibility

- The old leaf-trailing positional help shortcut (for example,
  `icq sns list help`) is removed as a pre-1.0 hard cut. Use `-h`, `--help`, or
  the native `icq help <path>` form.
- Command names, report options and JSON, text report content, exit-code
  classes, network authority, cache identity and schemas, refresh behavior, and
  live calls are unchanged. Exact Clap-generated help and parse-diagnostic
  layout may differ.
- No compatibility parser, alias, hidden live call, or mutation behavior is
  added.

## 0.22.0 - 2026-08-01

### Changed

- Establishes the `0.22.x` structural-consolidation design and its ordered
  validation gates for CLI composition, NNS Registry inventory orchestration,
  snapshot lifecycle sharing, and cohesive module ownership.
- Replaces the NNS neuron family's wildcard public re-export with an explicit
  current-contract export list, preventing future internal additions from
  silently expanding the family-root Rust API.
- Consolidates repeated non-mainnet topology command tests into one
  table-driven contract while retaining coverage for every supported topology
  view and refresh operation.
- Adds a focused `subnet-catalog-host` library feature for embedders that need
  live/cache Subnet catalog behavior without the Dashboard Reqwest transport or
  CBOR certification dependency. The existing `host` feature remains a strict
  superset and the CLI continues to use it.
- Makes every repository-document and changelog link in the README absolute to
  GitHub, so the same README remains navigable from the published library and
  CLI archives even though those archives do not contain the root `docs/` and
  changelog trees.
- Centralizes the cache-missing refresh driver and exact-or-unique-prefix row
  resolver used by node, node-provider, node-operator, and data-center reports.
  The same internal owner now enforces the network contract, constructs shared
  source provenance, and orders fetch before locked atomic cache publication.
  Family DTOs, errors, sources, projections, cache files, filters, and refresh
  reports remain distinct.
- Centralizes NNS Governance proposal/neuron refresh-attempt construction,
  strict sidecar reads, metadata validation, report-status projection, and
  failed-progress recovery. The existing generic snapshot owner continues to
  own locking, paging progress, completeness, atomic publication, and attempt
  finalization; proposal and neuron public errors and cache contracts remain
  distinct.
- Centralizes SNS proposal/neuron target lookup, network validation, refresh
  locking, resolved context, complete-cache provenance and completeness,
  atomic publication, page-progress evidence, and attempt finalization. The
  family-specific proposal and neuron cursor logic, row validation, source
  traits, cache schemas, public reports, and errors remain separate; four
  redundant context/progress leaf modules are removed.

### Compatibility

- This cleanup does not change public Rust paths or types, CLI grammar or
  output, report JSON, cache paths or schemas, network calls, refresh policy,
  or source-adapter behavior.
- Enabling `subnet-catalog-host` exposes the same Subnet catalog API available
  under `host`; it does not introduce a second implementation or behavior
  branch.
- No compatibility aliases, fallback readers, deprecated wrappers, mutation
  behavior, or hidden live calls are added.
