# 0.25 Changelog

Detailed release notes for the `0.25.x` release line.

Root summary: [../../CHANGELOG.md](../../CHANGELOG.md)

When a patch introduces any new CLI surface, include a fenced `bash` command
example here and in the root changelog. New CLI surface includes commands,
subcommands, options, option values, and new supported option combinations.
Cleanup-only patches without CLI behavior changes should not include command
examples.

## 0.25.26 - 2026-08-03

### Changed

- Adds shared typed storage helpers that discover and load the unique complete
  SNS cache by stable list id or derive and load it by Root canister principal.
- Reduces proposal and neuron cached-input adapters to their intentionally
  different network validation and typed missing-id behavior.
- Deletes family-local id lookup functions and the neuron lookup-error module;
  invalid principal input now propagates the identical `InvalidLookup` error
  already produced by the shared principal parser.

### Compatibility

- Public Rust APIs, cache paths and schemas, strict header validation,
  ambiguous-id handling, proposal and neuron missing-cache variants, invalid
  lookup errors, source calls, reports, and CLI behavior are unchanged.

## 0.25.25 - 2026-08-02

### Changed

- Makes shared SNS cache-list construction generic over the collection storage
  marker, so discovery, summary loading, ordering, and report assembly no
  longer require family-local callbacks.
- Removes the separate cache-status family trait, path DTO, and proposal and
  neuron implementations. Shared status lookup now derives cache discovery,
  header reads, root paths, summaries, collection identity, and strict attempt
  evidence directly from the storage marker.
- Reduces the proposal and neuron cache list/status modules to their public
  entry points, schema versions, and collection marker selections.

### Compatibility

- Public Rust APIs, cache paths and schemas, list ordering, id/root lookup,
  missing and malformed cache handling, strict refresh-attempt evidence, JSON
  and text output, source calls, and CLI behavior are unchanged.

## 0.25.24 - 2026-08-02

### Changed

- Extends the SNS cache-storage contract with each collection’s payload type,
  pagination item name, row count, and family-specific row validation.
- Centralizes complete-cache key construction, JSON loading, completeness and
  point-in-time validation, shared metadata validation, and payload validation
  in one typed storage function.
- Deletes the proposal and neuron `load.rs` modules and removes loader-function
  plumbing from summary, list, status, and lookup flows. Collection-specific
  cache aliases now derive their payload directly from the storage contract.

### Compatibility

- Public Rust APIs, cache paths and schemas, validation order and messages,
  typed cache errors, valid and invalid cache summaries, JSON and text output,
  lookup behavior, source calls, and CLI behavior are unchanged.

## 0.25.23 - 2026-08-02

### Changed

- Adds one internal SNS cache-storage contract associating each collection
  marker with its schema version, supported field allowlist, and typed
  missing-cache error.
- Uses that contract for snapshot discovery, header validation, complete-cache
  loading, and incomplete-refresh diagnostics instead of selecting proposal or
  neuron behavior through a separate enum.
- Deletes the duplicate proposal and neuron scan modules and their redundant
  collection-specific header aliases. Reports and storage lookups now use the
  shared typed discovery and header readers directly.

### Compatibility

- Public Rust APIs, cache paths and schemas, supported-field validation, typed
  cache errors, incomplete-refresh diagnostics, JSON and text output, lookup
  behavior, source calls, and CLI behavior are unchanged.

## 0.25.22 - 2026-08-02

### Changed

- Replaces the duplicated proposal and neuron cache-summary storage modules
  with shared typed loading and projection over their common SNS snapshot
  envelope and metadata.
- Removes the cache-summary projection macro. Valid and invalid snapshots now
  use ordinary typed functions for cache status, identity, completeness,
  refresh-attempt evidence, and source provenance.
- Routes proposal and neuron cache-list and cache-status reports directly
  through the shared summary owner and their family-specific cache loaders.

### Compatibility

- Public Rust APIs, cache paths and schemas, valid and invalid summary fields,
  refresh-attempt evidence, JSON and text output, ordering, lookup behavior,
  network validation, source calls, and CLI behavior are unchanged.

## 0.25.21 - 2026-08-02

### Changed

- Centralizes concrete SNS cache-list and cache-status report assembly for the
  proposal and neuron cache families. The shared list builder accepts each
  family’s storage function directly, while the status-family trait exposes
  only its collection marker and storage operations instead of repeating
  summary types, attempt types, network-directory hooks, collection names, and
  public report projection.
- Removes the internal generic summary-view trait; shared cache ordering,
  lookup, and error rendering now operate directly on `SnsCacheSummary`.
- Removes six one-line proposal and neuron cache-path forwarders. Both storage
  families now use the shared SNS network-directory, refresh-attempt, and
  snapshot-key helpers directly with their existing collection markers.

### Compatibility

- Public Rust APIs, cache paths and keys, cache and report schemas, JSON and
  text output, list ordering, status lookup and attempt behavior, network
  validation, live source calls, and CLI behavior are unchanged.

## 0.25.20 - 2026-08-02

### Changed

- Removes the test-only Subnet stale-duration parser and its test-only host
  error variant. Duration syntax remains owned and tested by the shared parser
  used by supported CLI options, while refresh-failure fixtures now return a
  real catalog-validation error.
- Narrows the compact Subnet-principal renderer to its text module and removes
  the two test-only re-exports that exposed it to sibling tests. Report tests
  continue to verify compact text through the public renderer output.
- Removes ten NNS proposal and Subnet constants that duplicated CLI values only
  for tests, together with their re-export chain. Focused tests now state the
  supported command values and parsed defaults directly.
- Deletes the CLI test-support module that wrapped one SNS help snapshot
  assertion and the one-call NNS proposal match-parser adapter.
- Keeps the ICRC tip-certificate fixture verifier inside its test module rather
  than exposing a test-only function and import at the production module
  boundary.

### Compatibility

- Production command grammar, defaults, diagnostics, dispatch, public Rust
  APIs, host errors, report JSON, text output, cache behavior, source calls,
  and network behavior are unchanged. The removed host error variant existed
  only in test builds and could not be returned by published code.

## 0.25.19 - 2026-08-02

### Changed

- Removes the shared test-only parser that cloned a Clap command and appended
  its complete rendered help to every parse error. Dashboard, ICRC, NNS, SNS,
  and System tests now use the existing production parser and retain its native
  diagnostic in their typed test errors.
- Removes three System help-rendering forwarders; focused tests render the
  production System and report command builders directly.
- Reuses the NNS test match parser for the fallible proposal projection instead
  of maintaining a second raw-match adapter.

### Compatibility

- Production parsing, command grammar, defaults, help and diagnostics,
  dispatch, public Rust APIs, report JSON, text output, cache behavior, source
  calls, and network behavior are unchanged.

## 0.25.18 - 2026-08-02

### Changed

- Deletes the ICRC test-support facade, including thirteen type-specific option
  parsing adapters and eighteen help-rendering forwarders.
- Exercises ICRC ledger, account, account-history, and cache commands through
  one generic test parser, the production command builders, and their existing
  typed `from_matches` projections.
- Makes those builders and projections visible only within the private `icrc`
  module so its sibling tests can use the production owners directly.

### Compatibility

- Production command grammar, defaults, help and diagnostics, option
  projection, dispatch, public Rust APIs, report JSON, text output, cache
  behavior, source calls, and network behavior are unchanged.

## 0.25.17 - 2026-08-02

### Changed

- Removes twelve SNS option methods that existed only to construct Clap matches
  for tests, along with their dedicated parse-error adapter module.
- Deletes the SNS test-only usage module and its twenty-two help-rendering
  forwarders.
- Reuses shared SNS test harnesses for infallible and validated option
  projections, ordinary string-slice arguments, and direct rendering of the
  production command builders.

### Compatibility

- Production command grammar, defaults, help and diagnostics, option
  projection and validation, dispatch, public Rust APIs, report JSON, text
  output, cache behavior, source calls, and network behavior are unchanged.

## 0.25.16 - 2026-08-02

### Changed

- Removes the test-only generic NNS leaf-helper macro and the 21 option/help
  adapters it generated for node-provider, node-operator, and data-center
  commands.
- Removes the generic leaf and node-only option parsers, help modules, and
  top-level NNS parsing/help forwarders that existed only to support focused
  tests.
- Exercises node, node-provider, node-operator, and data-center parsing and
  help directly through the production Clap builders and existing shared NNS
  test harness.

### Compatibility

- Production command grammar, defaults, help and diagnostics, option
  projection, dispatch, public Rust APIs, report JSON, text output, cache
  behavior, source calls, and network behavior are unchanged.

## 0.25.15 - 2026-08-02

### Changed

- Removes three Subnet and nine Topology option parsers that existed only for
  tests. The Topology option-definition macro no longer accepts or emits
  test-command wiring.
- Deletes the Subnet and Topology test-only usage modules and their fourteen
  help-rendering wrappers. Focused tests render the production command builders
  directly.
- Reuses the shared NNS production-Clap harness for ordinary string-slice
  parsing across every migrated option type.

### Compatibility

- Production command grammar, defaults, help and diagnostics, option
  projection, dispatch, public Rust APIs, report JSON, text output, cache
  behavior, source calls, and network behavior are unchanged.

## 0.25.14 - 2026-08-02

### Changed

- Removes six option methods that existed only to parse NNS neuron, Governance,
  and Registry unit-test arguments.
- Reuses one NNS production-Clap parsing harness across those commands and the
  infallible proposal detail, refresh, and cache option projections.
- Removes two layers of test-only help forwarding for the neuron family plus
  the Governance and Registry help forwarders. Tests now render every supported
  command builder directly.

### Compatibility

- Production command grammar, defaults, help and diagnostics, option
  projection, dispatch, public Rust APIs, report JSON, text output, cache
  behavior, source calls, and network behavior are unchanged.

## 0.25.13 - 2026-08-02

### Changed

- Builds the supported NNS proposal list and info commands directly instead of
  retaining configurable helpers with only one call site each.
- Removes proposal option methods and help-rendering forwarders that existed
  only for tests. Focused tests now parse ordinary string slices through the
  production Clap commands and render those command builders directly.

### Compatibility

- Production command grammar, defaults, help and diagnostics, option
  projection, dispatch, public Rust APIs, report JSON, text output, cache
  behavior, source calls, and network behavior are unchanged.

## 0.25.12 - 2026-08-02

### Changed

- Removes five repeated test-only Dashboard option parsers plus the separate
  canister-count parsing flow.
- Uses one IC-family test harness to parse ordinary string slices through the
  selected production Clap command and `from_matches` projection for canister,
  metric, and network-resource options.

### Compatibility

- Production command parsing, grammar, defaults, help and diagnostics, public
  Rust APIs, report JSON, text output, request bounds, source calls, cache
  behavior, and network behavior are unchanged.

## 0.25.11 - 2026-08-02

### Changed

- Reuses `IcrcLedgerOptions` for the ledger id, source endpoint, and output
  selection shared by live balance, allowance, ledger-transaction, and archive
  commands instead of parsing and storing those fields independently.
- Keeps account-history cache target identity and its view/collection options
  separate because cache keys describe collected data rather than output
  selection.

### Compatibility

- Command grammar, defaults, help and parse diagnostics, dispatch requests,
  public Rust APIs, report JSON, text output, cache behavior, source calls, and
  network behavior are unchanged.

## 0.25.10 - 2026-08-02

### Changed

- Replaces the mixed ICRC account-history cache module with a small facade over
  dedicated strict-storage, refresh/publication, local-report, and existing
  refresh-attempt owners.
- Keeps normalized cache identity, path hashing, strict JSON validation, and
  staleness checks together in storage; refresh locking, source collection,
  completion validation, atomic publication, and missing/stale policies now
  share the refresh owner.
- Keeps cache-only list and status projection separate from every live path,
  while attempt evidence retains its existing sidecar owner.
- Centralizes incomplete-collection error construction so zero-page, final-
  cursor, and canonical-row validation failures retain the same resolved-index
  and progress evidence.

### Compatibility

- Public Rust paths and types, cache keys, paths and schemas, refresh lock and
  attempt behavior, missing/stale policies, canonical validation, report JSON,
  text output, CLI behavior, source calls, and network behavior are unchanged.

## 0.25.9 - 2026-08-02

### Changed

- Removes repeated test-only `parse` methods and their command-builder imports
  from the typed ICRC option models.
- Uses one test-support helper to build the selected Clap command, parse test
  arguments, and project matches through the production `from_matches`
  implementation.

### Compatibility

- Production option parsing and diagnostics, command grammar and help, public
  Rust APIs, report JSON, text output, cache behavior, source calls, and
  network behavior are unchanged.

## 0.25.8 - 2026-08-02

### Changed

- Separates ledger-wide ICRC Clap command construction from account and
  account-history command construction behind the existing ICRC command
  facade.
- Keeps the facade responsible for shared argument validators and root command
  composition, while typed option extraction and dispatch remain in their
  dedicated owners.
- Removes test-only usage forwarding functions; command tests render the
  actual Clap builders directly.

### Compatibility

- Command hierarchy, names, options, defaults, help text, parse diagnostics,
  public Rust APIs, report JSON, text output, cache behavior, source calls, and
  network behavior are unchanged.

## 0.25.7 - 2026-08-02

### Changed

- Uses one optional-subaccount normalizer for ICRC balance, allowance, live
  account-transaction page, and complete account-history cache requests.
- Reuses the existing typed ICRC principal conversion for cached ledger,
  account-owner, explicit-index, and completed-collection identities instead
  of repeating the same error mapping.
- Confirms that uppercase and lowercase forms of one subaccount resolve to the
  same complete-cache identity.

### Compatibility

- Accepted account targets, typed errors and validation order, public Rust and
  CLI APIs, report JSON, text output, cache paths and schemas, source calls,
  and network behavior are unchanged.

## 0.25.6 - 2026-08-02

### Changed

- Replaces repeated SNS-W inventory, metadata, Root canister, metrics, swap,
  upgrade, and neuron source-error construction with one internal
  capability-aware validator.
- Uses that shared validator for exact source evidence and canonical principal
  text while retaining each capability's protocol-specific validation and
  typed error label.

### Compatibility

- Public Rust paths and types, CLI behavior, validation order and errors,
  report JSON, text output, source calls, and network behavior are unchanged.

## 0.25.5 - 2026-08-02

### Changed

- Centralizes the canonical lowercase-hex predicates used by neuron ids, Wasm
  hashes, fixed-size SNS treasury subaccounts, Dashboard module hashes, and CMC
  certificate evidence without changing their field-specific errors or length
  contracts.
- Corrects the `subnet-catalog-host` documentation and dependency gate: the
  feature leaves `ic-query`'s direct optional `reqwest` and `serde_cbor` edges
  disabled, while those packages may remain transitively through `ic-agent`.
- Compiles and exercises the public Subnet catalog API with only the focused
  feature and checks its direct normal dependency boundary separately from the
  complete transitive graph.
- Simplifies shared internal feature conditions to use
  `subnet-catalog-host` as their capability gate. The full `host` feature
  continues to enable that focused feature and add the remaining reporting
  dependencies.

### Compatibility

- Public Rust paths and types, CLI behavior, validation errors, report JSON,
  text output, cache behavior, source calls, and network behavior are
  unchanged.

## 0.25.4 - 2026-08-02

### Changed

- Separates live ICRC token, balance, allowance, and structured-account
  adaptation from index discovery, ICRC-3 ledger history, archive traversal,
  block conversion, and tip-certificate fetching.
- Gives bounded capability probing its own owner while reusing the same
  low-level index, history, archive, block-type, and certificate query
  primitives as the primary reports.
- Keeps shared agent construction, ledger-principal parsing, and internal
  fetch exports behind one facade used by the synchronous live source and
  account-transaction collection.

### Compatibility

- Public Rust paths and types, CLI behavior, report JSON, text output, endpoint
  validation, source-call counts, account-history behavior, archive callback
  traversal, capability status projection, and tip-certificate verification
  are unchanged.

## 0.25.3 - 2026-08-01

### Changed

- Separates official Dashboard canister, metrics, and network-resource command
  construction, typed option parsing, execution, and focused tests into their
  cohesive CLI owners behind the existing `ic` facade.
- Keeps the facade responsible only for the shared error type, root command
  composition, and family dispatch.

### Compatibility

- CLI commands and help, public Rust APIs, report JSON, text output, request
  bounds, endpoint defaults, source-call counts, cache behavior, and network
  behavior are unchanged.

## 0.25.2 - 2026-08-01

### Changed

- Separates the NNS neuron cache model, paths, refresh-attempt evidence, page
  collection, publication, refresh orchestration, and cached report projection
  into cohesive internal owners behind the existing cache facade.
- Keeps the neuron-specific public Governance row validation, ordered cursor
  walk, API-exhaustion requirement, atomic publication, refresh lock, attempt
  evidence, cache-only list/detail projection, and status reporting intact.

### Compatibility

- Public Rust paths and types, CLI behavior, report JSON, cache paths and
  schemas, validation and error mapping, text output, refresh policy, progress
  events, source calls, and network behavior are unchanged.

## 0.25.1 - 2026-08-01

### Changed

- Corrects README and CLI documentation that described every report schema as
  version 1 now that SNS neuron reports and caches use version 2.
- Shares the identical optional-value, case-insensitive text, and directional
  ordering primitives used by NNS and SNS proposal views while retaining their
  separate filters and report semantics.
- Moves generic optional scalar text rendering to the shared text-value owner
  and removes a redundant SNS proposal wrapper.
- Folds the one-function dispatch and one-macro test-helper leaves for NNS data
  centers, node operators, and node providers into their owning command
  modules.

### Compatibility

- Public APIs, CLI behavior, report JSON, cache paths and schemas, sorting,
  text output, source calls, and network behavior are unchanged.

## 0.25.0 - 2026-08-01

### Added

- Expands `SnsNeuronRow` and the existing `sns neuron list` and refresh flows
  with the fixed-size native Governance fields already returned by
  `list_neurons`: source NNS neuron id, auto-stake maturity, aging timestamp,
  dissolve state, voting-power percentage multiplier, vesting period, and
  neuron fees.
- Adds the public `SnsNeuronDissolveState` model using the native
  `DissolveDelaySeconds` and `WhenDissolvedTimestampSeconds` alternatives.
- Preserves every added field as raw JSON evidence. Text output remains a
  compact view over that complete row.

### Changed

- Applies one canonical neuron-row validator to bounded custom-source results,
  complete refresh pages, and loaded caches. It enforces lowercase even-length
  id hex, exact derived creation-time text, unique ids, and the requested live
  limit.
- Validates cache schema and network headers before decoding the complete row
  model. An older schema therefore returns the typed unsupported-schema error
  even when its row shape no longer deserializes as the current model.

### Bounds And Compatibility

- Adds no query, follow-up, pagination, fanout, or cache write beyond the
  existing bounded `list_neurons` and explicit complete-refresh flows.
- Deliberately omits variable-size permission, followee, topic-followee, and
  pending maturity-disbursement graphs from collection rows. A later detail
  report may expose those through one explicit neuron lookup.
- Advances SNS neuron report and cache schemas to version 2. Version-1 neuron
  caches are rejected as unsupported and require an explicit refresh; there
  is no migration, fallback reader, alias, or legacy row shape.
